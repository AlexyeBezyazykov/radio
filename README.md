## 🎧 radioctl — консольное интернет-радио для Linux

radioctl — простой консольный интернет-радиоплеер для Linux на базе VLC (cvlc)
с автозапуском через systemd user service и выводом звука через PulseAudio.

Проект создавался как максимально простое и надёжное решение для фонового
проигрывания интернет-радио (сервер, мини-ПК, домашний компьютер).

## ✨ Возможности

▶ Проигрывание интернет-радио

⏭ Переключение станций

⏹ Остановка воспроизведения

ℹ Проверка статуса

🔁 Автозапуск при входе пользователя

🔊 Работа через PulseAudio (без sudo)

🧩 Простая конфигурация в текстовых файлах

## 📦 Зависимости
    sudo apt update
    sudo apt install vlc pulseaudio -y

## 🚀 Установка

   ```git clone      https://github.com/USERNAME/radioctl.git
   cd radioctl
   chmod +x install.sh
   ./install.sh
```

⚠ Установка выполняется под обычным пользователем, не под sudo.

## 🎮 Управление
```radioctl play     # Запуск радио
radioctl stop     # Остановка
radioctl next     # Следующая станция
radioctl status   # Статус воспроизведения
```

## 📻 Список радиостанций

Файл со станциями:
```
~/.config/radio/stations.list
```

Формат строк:
```
Название|URL
```

Пример:
```
Record|https://radiorecord.hostingradio.ru/rr_main96.aacp
Chill|https://radiorecord.hostingradio.ru/chil96.aacp
Lo-Fi House|https://radiorecord.hostingradio.ru/lofihouse96.aacp
Summer Lounge|https://radiorecord.hostingradio.ru/summerlounge96.aacp
```
## ⚙ systemd (user service)

Сервис устанавливается как пользовательский, а не системный.

Проверка статуса:
```
   systemctl --user status radio.service
```

Перезапуск:
```
   systemctl --user restart radio.service
```

Включение автозапуска:
```
systemctl --user enable radio.service
```

## 🔁 Автозапуск после перезагрузки

Для работы без активного логина должен быть включён linger:
```
loginctl enable-linger $USER
```

## Проверка:
```
loginctl show-user $USER | grep Linger
```

## 🔊 Про звук

Используется PulseAudio

cvlc запускается без GUI

Работает без sudo

Проверка PulseAudio:
```
pactl info
```

## 🛠 Устранение проблем
❌ Нет звука

Убедитесь, что пользователь в группе audio

Проверьте, что PulseAudio запущен

Проверьте, что radioctl play работает вручную

